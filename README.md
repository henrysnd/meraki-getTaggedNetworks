taggednetworks.py grabs the network ID based on organization tags, use more tags to narrow down the scope of the networks to be updated.

createl7firewallrules.py is an example to add the layer 7 rules based using taggednetworks.py to find the network ID
